<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GrowUp Computer Training Center | Jaggaiahpet</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
    <style>
        body { background: #0f172a; color: white; overflow-x: hidden; }

        /* Glassmorphism Effect */
        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        .glass-card:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: #3b82f6;
            transform: scale(1.05);
            box-shadow: 0 20px 25px -5px rgba(59, 130, 246, 0.5);
        }

        /* High-Tech Gradients */
        .hero-gradient {
            background: radial-gradient(circle at top right, #1e40af, transparent),
                        radial-gradient(circle at bottom left, #7e22ce, transparent);
        }
        .text-gradient {
            background: linear-gradient(to right, #60a5fa, #a855f7);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    </style>
</head>
<body class="hero-gradient min-h-screen">

    <%- include('partials/header') %>

    <section class="container mx-auto px-6 py-24 text-center">
        <h1 class="text-5xl md:text-8xl font-extrabold mb-6 animate__animated animate__zoomIn">
            Master the <span class="text-gradient">Future</span>
        </h1>
        <p class="text-gray-100 text-lg md:text-2xl max-w-3xl mx-auto mb-12 animate__animated animate__fadeIn animate__delay-1s font-medium">
            Join <strong class="text-white">GrowUp Computer Training Center</strong> in Seetharam Puram, Jaggaiahpet.
            Industry-leading courses designed to build your tech empire.
        </p>
        <div class="animate__animated animate__fadeInUp animate__delay-1s">
            <a href="#courses" class="bg-blue-600 hover:bg-blue-500 text-white px-10 py-5 rounded-full font-bold text-lg transition-all shadow-lg shadow-blue-500/50 uppercase tracking-tight">
                Explore Our Courses
            </a>
        </div>
    </section>

    <section id="courses" class="container mx-auto px-6 py-20">
        <div class="flex flex-col items-center mb-16">
            <h2 class="text-4xl font-bold mb-4 text-white">Specialized Programs</h2>
            <div class="w-24 h-1 bg-gradient-to-r from-blue-500 to-purple-500 rounded-full"></div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
            <% Object.keys(courses).forEach(key => { %>
                <a href="/course/<%= key %>" class="glass-card p-10 rounded-[2rem] block group">
                    <div class="w-14 h-14 bg-blue-500/20 rounded-2xl flex items-center justify-center mb-8 group-hover:bg-blue-500/40 transition-colors">
                        <svg class="w-8 h-8 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                        </svg>
                    </div>

                    <h3 class="text-3xl font-bold mb-4 text-white group-hover:text-blue-300 transition-colors"><%= courses[key].title %></h3>
                    <p class="text-gray-200 mb-8 leading-relaxed line-clamp-3 font-medium">
                        <%= courses[key].description %>
                    </p>

                    <div class="flex items-center text-blue-400 font-bold group-hover:text-blue-200 transition-colors">
                        Learn More
                        <svg class="w-5 h-5 ml-2 transform group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                        </svg>
                    </div>
                </a>
            <% }) %>
        </div>
    </section>

<%- include('partials/footer') %>

</body>
</html>